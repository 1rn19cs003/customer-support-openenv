name: customer-support-env
description: Customer support ticket resolution environment
version: 0.1.0

# Add these to help the automated validator
tasks:
  - id: easy
    difficulty: 0.1
  - id: medium
    difficulty: 0.5
  - id: hard
    difficulty: 1.0

endpoints:
  reset: /reset
  step: /step
  state: /state